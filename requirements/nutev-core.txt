# NutEV/NutMEV CORE runtime dependencies.
#
# This is the minimal set required to import `nutev`, parse config, generate
# demo data, run a basic bibliographic search and read/write tabular outputs.
# It mirrors [project.dependencies] in pyproject.toml. Prefer installing via
# `pip install -e .` (which reads pyproject); this file exists for pinned
# environments and mirrors/audits.
#
# Deliberately excludes: Flask, frontend, Elasticsearch, FAISS, Playwright,
# SQLCipher and the LangChain ecosystem. Those live in optional extras
# (see docs/DEPENDENCY_ARCHITECTURE.md).
pandas~=3.0
pydantic~=2.12
requests>=2.32
beautifulsoup4~=4.14
python-dateutil~=2.9
openpyxl~=3.1
typing-extensions~=4.15
