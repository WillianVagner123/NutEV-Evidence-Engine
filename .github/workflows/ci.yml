name: ci

on:
  push:
    branches: ["**"]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  tests:
    name: tests (python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12", "3.13"]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install project and test dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements/nutev-ci.txt
          python -m pip install --no-deps -e .
      - name: Run Reference Engine tests
        run: PYTHONPATH=src python -m pytest -q nutev_tests

  guardrail-contract:
    name: audit guardrail contract
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
      - name: Install project and test dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements/nutev-ci.txt
          python -m pip install --no-deps -e .
      - name: Verify fail-closed ranking contract
        run: PYTHONPATH=src python -m pytest -q nutev_tests/test_rank_references.py

  windows-smoke:
    name: windows smoke (python 3.12)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
      - name: Install project
        shell: pwsh
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements/nutev-ci.txt
          python -m pip install --no-deps -e .
      - name: CLI smoke
        run: |
          nutev --version
          nutev providers
      - name: Compile current product
        run: python -m compileall -q src tools
      - name: Ranking smoke without network
        run: python -m pytest -q nutev_tests/test_reference_queries.py nutev_tests/test_rank_references.py

  typecheck-provenance:
    name: typecheck provenance core
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
      - name: Install type-check dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements/nutev-ci.txt
          python -m pip install --no-deps -e .
      - name: Mypy Reference Engine provenance core
        run: >-
          mypy --ignore-missing-imports --check-untyped-defs
          src/nutev/audit_guardrails.py
          src/nutev/search/reference_queries.py
          src/nutev/search/official_sources.py

  lint:
    name: lint (ruff)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
      - name: Compile Python source and tools
        run: python -m compileall -q src tools nutev_tests
      - name: Install ruff
        run: python -m pip install ruff
      - name: Ruff blocking errors
        run: ruff check src tools nutev_tests --select F,E9
      - name: Ruff advisory style
        run: ruff check src tools nutev_tests || true

  hetzner-container-smoke:
    name: hetzner container smoke
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Validate Compose contract
        env:
          NUTEV_DOMAIN: nutev.example.invalid
          NUTEV_BASIC_AUTH_USER: ci
          NUTEV_BASIC_AUTH_HASH: ci-placeholder-hash
          NUTEV_OCR_LANG: eng+por
        run: |
          cp deploy/hetzner/.env.example deploy/hetzner/.env
          docker compose --env-file deploy/hetzner/.env -f deploy/hetzner/compose.yaml config >/dev/null
      - name: Build production image
        run: docker build -f deploy/hetzner/Dockerfile -t nutev-hetzner:ci .
      - name: Start image and verify health
        run: |
          container_id="$(docker run -d -p 127.0.0.1:8765:8765 nutev-hetzner:ci)"
          trap 'docker logs "$container_id" || true; docker rm -f "$container_id" >/dev/null 2>&1 || true' EXIT
          for attempt in $(seq 1 30); do
            if curl --fail --silent --show-error http://127.0.0.1:8765/api/health >/tmp/nutev-health.json; then
              cat /tmp/nutev-health.json
              exit 0
            fi
            sleep 1
          done
          exit 1
