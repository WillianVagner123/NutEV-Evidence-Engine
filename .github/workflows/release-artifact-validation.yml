name: release-artifact-validation

on:
  pull_request:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-and-clean-install:
    name: build, twine check, clean wheel install
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"

      - name: Verify release identity metadata are synchronized
        run: |
          python - <<'PY'
          import json
          import re
          from pathlib import Path

          ns = {}
          exec(Path("src/nutev/__version__.py").read_text(encoding="utf-8"), ns)
          expected = ns["__version__"]
          assert expected and ".dev" not in expected, expected
          assert json.loads(Path(".zenodo.json").read_text(encoding="utf-8"))["version"] == expected
          citation = Path("CITATION.cff").read_text(encoding="utf-8")
          versions = [v.strip() for v in re.findall(r'^\s*version:\s*["\']?([^"\'\n#]+)', citation, flags=re.MULTILINE)]
          assert versions and all(v == expected for v in versions), versions
          assert "NutEV Reference Engine" in Path("README.md").read_text(encoding="utf-8")
          print("release identity synchronized:", expected)
          PY

      - name: Build wheel and sdist
        run: |
          python -m pip install --upgrade pip
          python -m pip install build twine
          python -m build
          python -m twine check dist/*
          test -n "$(find dist -maxdepth 1 -name '*.whl' -print -quit)"
          test -n "$(find dist -maxdepth 1 -name '*.tar.gz' -print -quit)"

      - name: Install wheel in a clean virtual environment
        run: |
          python -m venv .release-venv
          .release-venv/bin/python -m pip install --upgrade pip
          WHEEL="$(find dist -maxdepth 1 -name '*.whl' -print -quit)"
          .release-venv/bin/python -m pip install "$WHEEL"
          .release-venv/bin/python -m pip check
          EXPECTED="$(python - <<'PY'
          ns = {}
          exec(open('src/nutev/__version__.py', encoding='utf-8').read(), ns)
          print(ns['__version__'])
          PY
          )"
          ACTUAL="$(.release-venv/bin/python -c "from importlib.metadata import version; print(version('nutev-nutmev'))")"
          test "$ACTUAL" = "$EXPECTED"
          .release-venv/bin/nutev --version
          .release-venv/bin/nutev providers

      - name: Audit public distribution boundaries
        run: |
          VERSION=$(python -c 'ns={};exec(open("src/nutev/__version__.py").read(),ns);print(ns["__version__"])')
          python tools/audit_public_package.py --dist dist --version "$VERSION" --output package_audit/distributions.json
      - name: Test installed wheel outside checkout
        run: |
          PYTHON="$(pwd)/.release-venv/bin/python"
          SMOKE="$(mktemp -d)"
          cp tools/installed_package_smoke.py "$SMOKE/check.py"
          cd "$SMOKE"
          "$PYTHON" -I check.py
      - name: Upload audited candidate distributions
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: audited-distributions-${{ github.sha }}
          path: |
            dist/*.whl
            dist/*.tar.gz
            package_audit/distributions.json
          if-no-files-found: error
          retention-days: 14

  container-validation:
    name: Container build, privacy and recovery
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
      - name: Run disposable container release audit
        run: bash tools/run_container_release_gate.sh
      - name: Upload container audit evidence
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: container-audit-${{ github.sha }}
          path: package_audit/
          if-no-files-found: error
          retention-days: 14
