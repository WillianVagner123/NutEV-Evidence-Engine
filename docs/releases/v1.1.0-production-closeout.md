# NutEV Reference Engine 1.1.0 — production closeout

Date: 2026-09-12 (America/Sao_Paulo)

## Canonical verdict

```text
SITE / SOFTWARE NUTEV 1.1.0 — PRODUCTION: PASS — CLOSED AND COMPLETE
```

Accepted production SHA:

```text
e40dfd8c48cde824fa6053f9b077157f21bae698
```

This document is the canonical operational closeout for NutEV 1.1.0. It closes site/software go-live and recovery acceptance. It does not claim that GitHub/Zenodo publication or A1/A2 scientific gates are complete.

## Final gate ledger

| Gate | State |
|---|---|
| 7/7 workflows on the same SHA | **PASS** |
| Recovery readiness on Hetzner | **PASS** |
| Snapshot-capacity gate | **PASS** |
| Three complete snapshots retained | **PASS** |
| Active release protected | **PASS** |
| Exact-SHA deploy `#1659` | **PASS** |
| NutEV 1.1.0 | **IN PRODUCTION** |
| External Caddy / ports 80–443 | **PASS** |
| `/search.html` and `/articles.html` | **200** |
| Private surfaces without auth | **401 / fail-closed** |
| Real backup / restore | **PASS** |
| Files verified | **19,466** |
| SQLite databases verified | **33** |
| `production_overwritten` | **false** |
| Post-deploy auditor `#99` | **PASS** |

## Auditor boundary

The final post-deploy audit executed and confirmed:

```text
read_only = true
scientific_state_modified = false
legacy_binding_performed = false
search_executed = false
A1 materialized = 0
A2 materialized = 0
```

Zero A1/A2 materializations are expected and correct. Production must not infer scientific approval or historical binding.

## Recovery and capacity

The permanent capacity remediation is intentionally scoped to unused Docker builder cache. It preserves:

- the active production release;
- three complete rollback snapshots;
- runtime images required by the release/recovery contract;
- the scientific data volume.

Final free-space evidence recorded 9,996,508 KB available against 4,825,220 KB required for the applicable snapshot gate.

## Scientific boundary

### Engine

The generic Engine remains under the scientific-validation protocol. Operational acceptance does not promote the scientific state.

### A1

A1 remains gated by real academic approval and human-review requirements. PR #1230 remains intentionally separate from platform go-live.

### A2

A2 remains fail-closed until genuine provenance and reviewed Legacy Binding evidence are available. No binding may be created by inference.

## Publication boundary

The production software is accepted, but publication is a distinct operation.

Until GitHub and archive publication are externally verified, use:

```text
PRODUCTION_ACCEPTED / PUBLICATION_PENDING
```

Publication requires:

1. immutable tag `v1.1.0` pointing exactly to `e40dfd8c48cde824fa6053f9b077157f21bae698`;
2. GitHub Release `v1.1.0`;
3. audited artifacts/release notes/hashes attached or preserved;
4. new Zenodo/archive record for version 1.1.0;
5. recording the DOI only after the archive service actually issues it.

The historical DOI:

```text
10.5281/zenodo.21998607
```

belongs to `v1.0.0` and must not be reused for `v1.1.0`.

## Backlog boundary

Historical PRs, Dependabot work and old search-expansion proposals are maintenance backlog, not blockers for this accepted production state. Triage them separately as:

```text
MERGE / SUPERSEDED / CLOSE / FUTURE
```

## Canonical references

- `README.md`
- `docs/FINAL_SYSTEM_ACCEPTANCE.md`
- `docs/SYSTEM_CLOSEOUT_MASTER.md`
- `docs/PUBLICATION_READINESS.md`
- `docs/ROLLBACK_RUNBOOK.md`
- `docs/HETZNER_AUTODEPLOY.md`
