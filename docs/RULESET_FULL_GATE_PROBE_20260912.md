# Ruleset full-gate probe — 2026-09-12

Temporary, non-mergeable verification artifact used only to prove that the `main` ruleset requires the complete product gate matrix.

This file must never be merged into `main`. The probe PR should be closed after the ruleset behavior is verified.
