# Archived Local Deep Research README

This file is intentionally retained as a short compatibility pointer.

The public-facing project identity is now:

```text
NutEV/NutMEV — Evidence Engine for Lifestyle Nutrition
```

Use the current project README instead:

```text
README.md
```

Historical Local Deep Research material is documented in:

```text
archive/legacy_local_deep_research/README.md
```

The canonical runtime for new doctorate work is:

```text
src/nutev/
```

Legacy Local Deep Research package metadata, commands and source modules may still exist temporarily for compatibility, but new NutEV/NutMEV development should not depend on them unless explicitly justified.
