# Auto detect text files and perform LF normalization
* text=auto

# Windows launchers need CRLF; shell launchers must stay LF.
*.bat text eol=crlf
*.ps1 text eol=crlf
*.command text eol=lf
*.sh text eol=lf
