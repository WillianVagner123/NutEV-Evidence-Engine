# NutEV/NutMEV no README principal

A documentação NutEV/NutMEV foi consolidada no `README.md` principal para evitar duplicação.

Use:

```bash
PYTHONPATH=src python -m nutev.cli --project-root ./project_output --workstreams busca1 busca2a busca2b a3 --web-enabled
```
